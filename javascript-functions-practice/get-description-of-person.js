/* exported getDescriptionOfPerson */
function getDescriptionOfPerson(person) {
  var desciptionOfPerson = person.name + ' is a ' + person.occupation + ' from ' + person.birthPlace + '.';
  return desciptionOfPerson;
}
